# supabase-biz

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build supabase-biz` to build the library.

## Running unit tests

Run `nx test supabase-biz` to execute the unit tests via [Jest](https://jestjs.io).
