# Project Tags Checklist

This file lists the exact `tags` to keep in each `project.json`.

## Apps

- `apps/admin/project.json`

```json
"tags": ["type:app", "product:admin", "platform:browser", "data:biz"]
```

- `apps/admin-e2e/project.json`

```json
"tags": ["type:e2e", "product:admin", "platform:browser"]
```

- `apps/ai-indexer/project.json`

```json
"tags": ["type:app", "product:ai-indexer", "platform:node", "data:public"]
```

- `apps/code-search/project.json`

```json
"tags": ["type:app", "product:code-search", "platform:node", "data:public"]
```

- `apps/marketing/project.json`

```json
"tags": ["type:app", "product:marketing", "platform:browser", "data:public"]
```

- `apps/secret-service/project.json`

```json
"tags": ["type:app", "product:secret-service", "platform:node", "data:secret"]
```

- `apps/supabase-biz/project.json`

```json
"tags": ["type:app", "product:supabase-biz", "platform:node", "data:biz"]
```

- `apps/supabase-secret/project.json`

```json
"tags": ["type:app", "product:supabase-secret", "platform:node", "data:secret"]
```

- `apps/web/project.json`

```json
"tags": ["type:app", "product:web", "platform:browser", "data:biz"]
```

- `apps/web-e2e/project.json`

```json
"tags": ["type:e2e", "product:web", "platform:browser"]
```

- `apps/worker/project.json`

```json
"tags": ["type:app", "product:worker", "platform:node", "data:secret"]
```

## Libs

- `libs/contracts/db-types-biz/project.json`

```json
"tags": ["type:lib", "scope:contracts", "layer:contracts", "platform:isomorphic", "data:biz"]
```

- `libs/contracts/db-types-secret/project.json`

```json
"tags": ["type:lib", "scope:contracts", "layer:contracts", "platform:isomorphic", "data:secret"]
```

- `libs/data-access/supabase-biz/project.json`

```json
"tags": ["type:lib", "scope:biz", "layer:data-access", "platform:node", "data:biz"]
```

- `libs/data-access/supabase-secret/project.json`

```json
"tags": ["type:lib", "scope:secret", "layer:data-access", "platform:node", "data:secret"]
```

- `libs/domain/billing/project.json`

```json
"tags": ["type:lib", "scope:biz", "layer:domain", "platform:isomorphic", "data:biz"]
```

- `libs/domain/secret/project.json`

```json
"tags": ["type:lib", "scope:secret", "layer:domain", "platform:isomorphic", "data:secret"]
```

- `libs/domain/tenant/project.json`

```json
"tags": ["type:lib", "scope:biz", "layer:domain", "platform:isomorphic", "data:biz"]
```

- `libs/shared/config/project.json`

```json
"tags": ["type:lib", "scope:shared", "layer:shared", "platform:isomorphic", "data:public"]
```

- `libs/shared/ui/project.json`

```json
"tags": ["type:lib", "scope:shared", "layer:ui", "platform:browser", "data:public"]
```

- `libs/shared/utils/project.json`

```json
"tags": ["type:lib", "scope:shared", "layer:shared", "platform:isomorphic", "data:public"]
```

- `libs/shared/validations/project.json`

```json
"tags": ["type:lib", "scope:shared", "layer:shared", "platform:isomorphic", "data:public"]
```

- `libs/testing/fixtures/project.json`

```json
"tags": ["type:lib", "scope:testing", "layer:testing", "platform:isomorphic", "data:public"]
```

- `libs/testing/utils/project.json`

```json
"tags": ["type:lib", "scope:testing", "layer:testing", "platform:isomorphic", "data:public"]
```
